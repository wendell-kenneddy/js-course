// For..in

/*
  The for in loop iterates over the properties of a object, like the indexes of
  a array or the keys of the object. It returns only the key, not the value.

  const myObj = {
    name: 'Wendell',
    nick: 'Kenneddy',
    age: 16
  };

  for (let key in myObj) {
    console.log(`The value of the key ${key} is ${myObj[key]}.`);
  }

  // -> The value of the key name is Wendell, The value of the key...
*/
