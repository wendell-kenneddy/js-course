// Importing modules using CommonJS

/*
  To import the module, we do require it:

  const { Person } = require('./app.js');

  This way, we import everything that app.js is exporting.

  const p1 = new Person('John', 16);
  console.log(p1.greeting()); // -> Hi, my name is John...
*/
