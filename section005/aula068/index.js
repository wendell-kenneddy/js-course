// Filter + map + reduce

/*
  const numArray = [10, 27, 38, 91, 80, 33, 41, 56, 72, 89, 100];
  const doubledEvenSum =
    numArray.filter(e => e %2 == 0)
      .map(e => e * 2)
      .reduce((ac, e) => ac + e);

  console.log(doubledEvenSum); // -> 712
*/
