// forEach

/*
  The forEach method executes a function to each element of an array. It
  receives a callback function, that can receive the value, the index and the
  source array as parameters.

  const numArray = [10, 3, 6, 9, 15, 21];
  numArray.forEach(e => console.log(e * 2)); // -> 20, 6, 12, 18, 30, 42
*/

