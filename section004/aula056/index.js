// IIFE (Immediately Invoked Function Expression)

/*
  IIFE's are functions called right after being created. IIFE functions don't
  pollute the global scope.

  const example = (function (name, nickname) {
    console.log(`Meu nome é ${name} ${nickname}.`)
  })('Wendell', 'Kenneddy');

*/
