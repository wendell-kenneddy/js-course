// Callback functions

/*
  Callback functions are functions passed to another function as argument, to be
  executed completing a certain routine or action (an interval, for example).

  const sayHi = callback => {
    if (callback) callback();
    console.log('Hi!');
  }

  sayHi(() => {
    console.log('Hello!');
  }) // -> Hello!...Hi!
*/

